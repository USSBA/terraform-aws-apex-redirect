# IPv4

## apex-redirect

This example provides a simple redirect from the apex, or root, of the domain to the appropriate subdomain using only IPv4 addresses. Elastic (static) IP addresses will be provisioned; however, allocation ids can be passed to the module.

